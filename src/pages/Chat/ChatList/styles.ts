import styled from "styled-components";

export const Container = styled.div`
    background-color: var(--chat-area);
    width: 30%;
    min-width: 300px;
    height: 100%;
    display: flex;
    flex-direction: column;
    border-right: var(--divise-chat) solid 1px;
`;
